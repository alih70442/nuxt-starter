import get_base_url from "assets/js/functions/route/get_base_url";

const ROUTE_BASE = get_base_url();

const ROUTE_LOCATIONS_CITIES = "cities";

const ROUTE_VERSION = "versions";
const ROUTE_CONFIG = "config";
const ROUTE_TRACKING = "tracking";

const ROUTE_LINK_SHOP = "shop";

export {
  ROUTE_BASE,
  ROUTE_VERSION,
  ROUTE_CONFIG,
  ROUTE_LOCATIONS_CITIES,
  ROUTE_TRACKING,
  ROUTE_LINK_SHOP,
};
